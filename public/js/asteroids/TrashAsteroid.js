import * as THREE from 'three';

export default class TrashAsteroid {
    constructor() {
        
    }

    // This function updates the asteroid movement through space
    update(t) {
        
    }
}